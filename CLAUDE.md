# 小喜桌宠 — AI 协作指南（CLAUDE.md）

本文件用于指导 AI 助手（Claude）在本项目中的工作方式。**每次新会话开始前必读本文件**。

---

## 项目速览

- **类型**：Windows 桌面宠物（喜乐蒂卡通形象）
- **技术栈**：Python 3.14 + PySide6 6.11 + openai SDK
- **目标**：可分发的轻量桌宠，含 AI 陪聊、提醒、动画交互
- **完整里程碑计划**：`C:\Users\Administrator\.claude\plans\windows-humming-squirrel.md`

---

## 标准文件索引（按任务类型查阅）

| 任务类型 | 必读文件 |
|---------|---------|
| 确认功能 / 接受新需求 | [docs/需求规格.md](docs/需求规格.md) |
| 技术选型 / 模块设计 | [docs/技术架构.md](docs/技术架构.md) |
| 写代码 | [docs/编码规范.md](docs/编码规范.md) |
| 处理 sprite / icon / 音频 | [docs/资源规范.md](docs/资源规范.md) |
| 生成 / 验收美术素材 | [docs/美术素材清单.md](docs/美术素材清单.md) |
| 启停 / 测试 / 验收 | [docs/开发流程.md](docs/开发流程.md) |
| 当前里程碑状态 | [docs/里程碑与验收标准.md](docs/里程碑与验收标准.md) |

---

## 工作方式（每次会话遵守）

### 会话开始
1. 读取本文件
2. 读取 `dev-log/` 下今天的日志（若不存在，按 `dev-log/_TEMPLATE.md` 新建）
3. 读取 `docs/里程碑与验收标准.md` 确认当前所处里程碑
4. 根据本次任务读取对应标准文件

### 会话结束（必做）
向今天的开发日志 `dev-log/YYYY-MM-DD.md` 追加：
- **完成事项**：列出文件改动、新增能力
- **待办事项**：用户已确认但尚未做的下一步
- **决策与阻塞**：技术选型、卡点、需要用户决策的事
- **测试结果**：是否人工验证通过

日期使用环境的 `Today's date` 字段。

### 桌宠启停
- 启动：双击项目根目录的 `启动小喜.bat`
- 退出：右键桌宠 → "退出小喜"
- 单实例锁已生效，重复双击不会启多只
- **代码改动后必须重启桌宠验证**（Stop-Process 旧的 → 启动新的）

启动属操作不属修改代码，但启动前先查是否已有 python 进程在跑，避免误判"启动失败"。

---

## 硬约束（违反前必须先与用户确认）

1. **不引入重依赖**：禁用 Electron / CEF / Node.js / Chromium / 大型 ML 框架。轻量优先是用户硬约束。
2. **不改启动器**：`启动小喜.bat` 是用户的日常入口，不要随意改动。
3. **不绕过单实例锁**：`SINGLE_INSTANCE_KEY` 是单实例的契约，改它等于破坏锁。
4. **不直接打包发布**：M5 才打包，之前不要执行 PyInstaller / Inno Setup。
5. **配置存储路径固定**：`%APPDATA%\XiLeDi\`，由 `src/core/paths.py:user_data_dir()` 统一返回。
6. **美术资源缺失时用占位图**：禁止因为没有真实美术而停下开发，用 `src/pet/placeholder_art.py` 顶上。

---

## 沟通约定

- 用中文回复
- 对小白友好：技术选型给理由 + 类比；不堆术语
- 阶段确认：方案先列选项给用户挑，确认后再动手
- 大改动前用 ExitPlanMode 或 AskUserQuestion 走流程
