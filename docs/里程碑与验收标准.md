# 里程碑与验收标准

> 本文件是每个里程碑的"完成定义"。每完成一项打 ☑，全部通过且用户确认后可推进下一个里程碑。

## 里程碑总览

| ID | 名称 | 状态 | 完成日期 |
|----|------|------|---------|
| M1 | 骨架 | ✅ 完成 | 2026-05-23 |
| M1.5 | 治理体系 | ✅ 完成 | 2026-05-23 |
| M2 | 动画 | ✅ 完成 | 2026-05-23 |
| M3 | 互动 | ✅ 完成 | 2026-05-23 |
| M4 | AI + 提醒 | ✅ 完成 | 2026-05-23 |
| M4.5 | 像素风格化 | ✅ 完成 | 2026-05-25 |
| M4.6 | 主动陪伴 + 学习陪伴 | ✅ 完成 | 2026-05-25 |
| M4.7 | Key 安全 + 历史可见性 | ✅ 完成 | 2026-05-25 |
| M4.8 | 美术与互动升级（扒边/拎脖/双食/咀嚼） | ✅ Phase A 完成 | 2026-05-26 |
| M4.9 | 互动打磨（5 bug + 喂食搭话 + 叼骨头 + 落地 + dizzy 完整显示 + peek wave） | ✅ Phase A 完成 | 2026-05-26 |
| M5 | 打包分发 | ✅ 完成 | 2026-05-26 |

---

## M1 骨架 ✅

**目标**：项目可启动并显示一只可拖动的占位桌宠。

### 验收清单
- ☑ 项目目录结构按 `docs/技术架构.md` 建立
- ☑ `requirements.txt` 含 PySide6 + openai
- ☑ `main.py` 可直接 `python main.py` 启动
- ☑ 桌宠窗口透明无白色边框
- ☑ 永远置顶
- ☑ 左键可拖动到屏幕任意位置
- ☑ 右键可看到"退出小喜"菜单并能成功退出
- ☑ 启动器 `启动小喜.bat` 双击即可启动
- ☑ 单实例锁：连续双击 .bat 仅 1 个进程
- ☑ 内存占用 < 150 MB（实测 43 MB）
- ☑ 用户人工验收通过

---

## M1.5 治理体系 ✅

**目标**：建立可追溯、稳定推进的项目治理机制。

### 验收清单
- ☑ `CLAUDE.md` 已建（项目根目录）
- ☑ `dev-log/_TEMPLATE.md` 模板已建
- ☑ `dev-log/2026-05-23.md` 含 M1 完成项回填
- ☑ `docs/需求规格.md`
- ☑ `docs/技术架构.md`
- ☑ `docs/编码规范.md`
- ☑ `docs/资源规范.md`
- ☑ `docs/美术素材清单.md`
- ☑ `docs/开发流程.md`
- ☑ `docs/里程碑与验收标准.md`（本文件）
- ☑ 用户人工验收通过（确认治理文件齐全后立即推进 M2）

---

## M2 动画 ✅

**目标**：让喜乐蒂活起来——播放多状态帧动画、自主巡游/坐下/睡觉、与拖动正确联动。

### 验收清单
- ☑ `src/pet/animator.py` 实现，读 manifest 加载帧 + QTimer 切帧
- ☑ `src/pet/state_machine.py` 实现 6 状态及合法转移
- ☑ `src/pet/behavior.py` 实现自主行为调度
- ☑ `src/assets/sprites/manifest.json` 含 6 个状态定义
- ☑ `placeholder_art.py` 扩展为帧级生成（缺真实素材时回落）
- ☑ `pet_window.py` 接入 Animator + Behavior，并改造拖动逻辑
- ☑ 桌宠空闲会自发切换 idle / walk / sit / sleep 状态
- ☑ walk 状态下桌宠平滑左右移动
- ☑ 走到屏幕边界自动回头
- ☑ 被拖动时不走路；落下播 dizzy 1 秒后回 idle
- ☑ 任务管理器观察：CPU < 2%、内存 < 100 MB
- ☑ 用户人工验收通过

---

## M3 互动 ✅

**目标**：完整的鼠标互动反馈 + 系统托盘 + 设置面板（含置顶开关）。

### 验收清单
- ☑ 右键菜单含：抚摸 / 喂食 / 睡觉 / 聊天 / 提醒 / 设置 / 退出
- ☑ 左键单击触发摇尾（happy 动画短促播放）+ 可选"汪"音效
- ☑ 左键双击打开 AI 聊天面板
- ☑ 抚摸：happy 动画 + 爱心粒子飘起
- ☑ 喂食：骨头掉落，宠物走过去"吃"
- ☑ 系统托盘存在，右键含：显示/隐藏 / 设置 / 退出
- ☑ 设置面板：置顶开关、音效开关、开机自启开关
- ☑ 置顶开关取消勾选后，桌宠可被其他窗口盖住
- ☑ `src/core/config.py` 实现 JSON 读写到 `%APPDATA%\XiLeDi\config.json`
- ☑ 用户人工验收通过

---

## M4 AI + 提醒 ✅

**目标**：能跟小喜聊天 + 定时提醒功能。

### 验收清单
- ☑ `src/ai/client.py` 实现 OpenAI 兼容流式调用（QThread）
- ☑ `src/ai/presets.py` 含 6 厂商预设（OpenAI / DeepSeek / 智谱 / 通义 / 月之暗面 / Ollama）
- ☑ `src/ai/persona.py` 含小喜角色 system prompt
- ☑ 设置面板：厂商下拉、base_url、api_key、model 输入框 + 测试连接
- ☑ 双击桌宠打开聊天面板
- ☑ 流式打字机效果显示 AI 回复
- ☑ 最近 20 轮对话保存到 `chat_history.json`
- ☑ 无 Key 时显示友好引导
- ☑ `src/core/scheduler.py` 实现 QTimer 调度
- ☑ 设置面板：增/删/启用/禁用提醒
- ☑ 提醒触发：桌宠原地弹气泡（不打扰用户）
- ☑ 用户人工验收通过

---

## M4.5 像素风格化 ✅

**目标**：把整个桌宠（角色 sprite + UI 面板 + 动效元素 + 字体）统一到 16-bit 像素风。

### 验收清单
- ☑ 接入用户提交的 28 个 AI 生图 PNG（24 帧 sprite + 4 单图）
- ☑ `src/ui/pixel_art.py` 实现 `render_pattern()` + `dewhite_pixmap()` 去白底
- ☑ `src/core/fonts.py` 注册中文像素字体 Fusion Pixel
- ☑ 桌宠 sprite Qt.FastTransformation 最近邻放大保持像素清晰
- ☑ 聊天面板 / 设置 / 菜单 / 提醒气泡 像素风 QSS（方角 + 像素字体 + 像素描边）
- ☑ 爱心 / 手掌 / 骨头 / 云朵 像素风
- ☑ 4 项 e3 体验修复（去白底、爱心避手掌、骨头落脚边、拖动入态切 dizzy）
- ☑ 内存 < 150 MB
- ☑ 用户人工验收通过

---

## M4.6 主动陪伴 + 学习陪伴 ✅

**目标**：让小喜从"被点才动"升级为"会陪伴的小伙伴"——主动搭话 + 学习陪伴锁定 + 周期鼓励。

### 验收清单
- ☑ `src/core/idle_chat.py` IdleChatter：QTimer 巡检 + 时段分类 + interval / quiet_enabled 控制
- ☑ `src/assets/data/idle_phrases.json` 30+ 条分类语料
- ☑ `src/core/pomodoro.py` PomodoroController：一次性专注 → 归零自动 stop
- ☑ `src/ui/progress_ring.py` 桌宠右上角进度环（48×48 像素圆）
- ☑ 设置面板"陪伴" tab：启用主动搭话 / 搭话时机 / 启用小喜休息时间 + 时段 / 启用学习陪伴 / 专注时长
- ☑ 右键菜单按 pomodoro.enabled 决定是否注入"启动/结束学习陪伴"
- ☑ 学习陪伴启动 → 小喜强制进入睡眠 + `_focus_lock` 拦截所有 behavior.resume 路径
- ☑ focus 剩余 ≤ 5 分钟 → 一次性提醒气泡"小主，学习时间不足5分钟啦，不要偷偷看手机哦！"
- ☑ focus 期间每 5 分钟随机鼓励气泡（最后 6 分钟内不打扰）
- ☑ focus 自然结束 → happy + "学习结束啦~ 小主辛苦啦 🌟" 庆祝气泡
- ☑ 主动搭话与学习陪伴互斥：focus 期间 chatter.pause
- ☑ bubble 跟随桌宠移动 + drag/show 时清场 + 唯一性（_show_bubble 入口）
- ☑ 设置面板复选框深棕填充 + ✓ 像素打钩字形 + ⓘ 悬停 tooltip（手动 QToolTip.showText）
- ☑ 桌宠尺寸 192→160；idle fps 6→3 降低眨眼频率
- ☑ 动作切换时长加倍（idle/walk/sit/sleep 范围 ×2）
- ☑ 设置面板打开时 dismiss 所有气泡避免遮挡
- ☑ 内存 < 150 MB（实测 72MB），空闲 CPU < 2%
- ☑ 用户人工验收通过

---

## M4.7 Key 安全 + 历史可见性 ✅

**目标**：把 API Key 从 config.json 明文移到 Windows 凭据管理器；聊天历史保留策略对用户可见。

### 验收清单
- ☑ `src/core/secrets.py` 封装 keyring 调用 + 回退兜底（keyring 不可用时降级到 config.json）
- ☑ `main.py` 启动时调 `secrets.migrate_from_config()`：旧版 config 里的明文 Key 自动迁移到凭据管理器
- ☑ 设置面板 AI tab 读 Key 走 `secrets.get_api_key()`，保存 Key 走 `secrets.set_api_key()`，ai_payload 不含 api_key
- ☑ 设置面板底部文案："Key 保存在 Windows 凭据管理器（服务名：XiLeDi），仅本机当前账户可读"
- ☑ 设置面板加"打开配置文件夹"按钮 → `os.startfile(%APPDATA%\XiLeDi\)`
- ☑ 聊天面板 `_has_api_key()` / `_on_send()` 通过 secrets 取 Key
- ☑ 聊天面板"清空历史"按钮左侧加灰字"保留最近 20 轮，更早的会自动清掉"
- ☑ requirements.txt 加 `keyring>=24.0`
- ☑ 用户人工验收通过

---

## M4.8 美术与互动升级 ✅（Phase A 完成 / Phase B 待美术）

**目标**：把"吸边姿态 / 拖动眩晕 / 喂食选项 / 咀嚼动画"四项体验升级。Phase A 是代码 + 占位图，Phase B 是替换正式美术。

### Phase A 验收清单（代码 + 占位图，本次交付）
- ☑ `src/pet/state_machine.py` STATES 扩展到 8 个（加 peek、eat）+ 转移表 + 最小停留时长
- ☑ `src/assets/sprites/manifest.json` + `src/pet/animator.py _DEFAULT_MANIFEST` 同步加 peek (3, fps2, loop) + eat (4, fps6, loop)
- ☑ `src/pet/placeholder_art.py` `make_placeholder_frame` 加 peek/eat 分支（视觉回落 idle/sit，状态机内是真 peek/eat）
- ☑ `src/pet/feeding.py` 重构：`_FoodWindow` 基类 + `BoneWindow` / `DogfoodWindow` 子类；`FeedingController.start(food_type)` 分流；bone 走原飞落 + walk_to，dogfood 直接落脚边；统一进入 `_begin_eat()` → 3s `eat` → 0.8s `happy` → `idle` 三段链路
- ☑ `src/ui/context_menu.py` 喂食改 QMenu 子菜单"🦴 骨头 / 🥣 狗粮"，回调签名拆 `on_feed_bone` + `on_feed_dogfood`
- ☑ `src/pet/pet_window.py` `_snap_to_edge` 改切 `peek` 状态、`_unhide_from_edge` 切回 `idle`；`_trigger_feed` 拆成 `_trigger_feed_bone` / `_trigger_feed_dogfood`，统一委托 `_start_feeding(food_type)`
- ☑ `docs/美术素材清单.md` 追加 M4.8 段（peek 3 帧 + dizzy 4 帧重做 + eat 4 帧 + dogfood.png 共 12 张图的 prompts）
- ☑ AST 解析通过；桌宠重启成功 PID 17468，内存 ~133MB（< 150MB 上限）
- ☑ 用户人工验收 Phase A 通过（与 M4.9 一/二/三/四轮一并验收，2026-05-26）

### Phase B 验收清单（美术到位后）
- ☐ `src/assets/sprites/peek/frame_01-03.png` 3 张就位
- ☐ `src/assets/sprites/dizzy/frame_01-04.png` 4 张覆盖旧版
- ☐ `src/assets/sprites/eat/frame_01-04.png` 4 张就位
- ☐ `src/assets/icons/dogfood.png` 1 张就位
- ☐ 重启桌宠，无需改代码，4 套素材自动接入：吸边真扒爪、拖动真"被拎"、喂狗粮真"低头吃碗"、喂骨头真"低头啃骨头"
- ☐ 资源占用 < 150MB、CPU < 2%
- ☐ 用户人工验收

---

## M4.9 互动打磨 ✅（Phase A 完成 / Phase B 待美术）

**目标**：M4.8 Phase A 用户验收时反馈的 5 个 bug + 4 个新增体验诉求。Phase A 是代码 + 占位图 + 9 张图 prompts，Phase B 是替换正式美术。

### Phase A 验收清单（代码 + 占位图，本次交付）

代码层（自动）：
- ☑ `src/pet/state_machine.py` STATES 从 8 扩展到 10（加 `hold_bone` / `fall`）+ 转移表 + 最小停留时长
- ☑ `src/assets/sprites/manifest.json` + `src/pet/animator.py _DEFAULT_MANIFEST` 同步加 hold_bone (4, fps 6, loop) + fall (2, fps 6, loop false)
- ☑ `src/pet/placeholder_art.py` `_MOOD_BY_STATE` 加 hold_bone→happy / fall→idle；`make_placeholder_frame` 加 hold_bone (bob 偏移) / fall (frame1 dy=+8, frame2 dy=0) 分支
- ☑ `src/core/idle_chat.py` `_load_phrases` 扩展 keys（含 after_feed_bone / after_feed_dogfood）；新增 `say_event(category)` 方法
- ☑ `src/assets/data/idle_phrases.json` 加 `after_feed_bone` + `after_feed_dogfood` 各 6 条语料
- ☑ `src/pet/feeding.py` 加 `eating_finished = Signal(str)` + `_food_type` 字段 + 构造函数注入 `animator`；`_start_dogfood` 用 `animator.direction()` 决定狗粮位置朝向（bug 3）；`_begin_eat` 按 food_type 分流到 `hold_bone` (骨头) / `eat` (狗粮)；`_on_happy_done` / `cancel` / `_on_walk_cancelled` 统一发 eating_finished 信号
- ☑ `src/pet/pet_window.py` `_EDGE_VISIBLE_PX` 54→90（bug 1+5）；常量 `_DRAG_RELEASE_DIZZY_MS` 800 改 `_DRAG_RELEASE_FALL_MS` 350（新 3）；`_start_feeding` 用 `lambda *_, ref=controller` 捕获引用绑定 destroyed（bug 4）；新增 `_on_feeding_destroyed(ref)` + `_on_feeding_finished(food_type)` 走 IdleChatter `say_event` 发反馈气泡（新 1）；删除旧 `_on_feeding_done`；`contextMenuEvent` 加 `_cancel_feeding()`（bug 2）；`mouseReleaseEvent` 拖动释放切 `fall` 状态 + 新方法 `_return_to_idle_if_fall_or_dizzy`（新 4）
- ☑ `docs/美术素材清单.md` 追加 M4.9 段：peek v2（覆盖 M4.8）+ hold_bone 4 帧 + fall 2 帧共 9 张图 prompts
- ☑ AST 解析 6 文件 + 2 JSON 通过；桌宠重启成功 PID 26428，内存 145MB（< 150MB 上限）；`STATES` 输出 10 个状态确认

UI 人工验收通过（2026-05-26，含一/二/三/四轮所有 bug 修复）：
- ☑ Bug 2：喂狗粮中右键 → 狗粮立即消失
- ☑ Bug 3：喂狗粮 → 碗出现在小喜面向那一侧脚边（不再左右随机错位）
- ☑ Bug 4：连续点喂食 2 次 → 第二次的食物正常显示（不再因 destroyed 时序丢失）
- ☑ 新 1：喂食结束 → 弹气泡（骨头 / 狗粮文案不同）
- ☑ 新 2：喂骨头 → 状态机切 hold_bone（占位视觉回落到 happy 偏移）
- ☑ 新 3：拖完小喜 ~350ms 回 idle（不再后摇 800ms）
- ☑ 新 4：拖完看到 fall 状态（占位视觉是基础图下移一帧再回正）
- ☑ 二轮：吸边时点喂食/睡觉/抚摸/聊天 → 弹"先把我拽出来"气泡，不切状态
- ☑ 二轮：peek 镜像方向修正（左缘看到角色扒在左墙，右缘扒在右墙）
- ☑ 二轮：PET_SIZE 160→128，整体更小
- ☑ 二轮：喂骨头吃完无 hold_bone → idle → happy 闪烁
- ☑ 三轮：dizzy sprite 1.4× 放大与其他状态视觉协调
- ☑ 三轮：喂狗粮吃完无 eat → idle → happy 闪烁
- ☑ 四轮：dizzy 图像完整显示（手掌 + 后爪都能看到），动态 resize 到 179×179
- ☑ 四轮：peek 招呼频率受控（frame_01/02 慢速循环，frame_03 18-35s 随机出现 + 头顶气泡）

### Phase B 验收清单（美术到位后）

- ☐ `src/assets/sprites/peek/frame_01-03.png` 3 张就位（**覆盖 M4.8 旧版**，斜侧扒墙 + 探头，占画布 70%+）
- ☐ `src/assets/sprites/hold_bone/frame_01-04.png` 4 张就位（叼着骨头甩头玩）
- ☐ `src/assets/sprites/fall/frame_01-02.png` 2 张就位（半空挣扎 → 着地缓冲）
- ☐ 重启桌宠，无需改代码：吸边真扒爪探头 + 喂骨头真叼骨头甩头 + 拖动释放真"摔落地"
- ☐ 资源占用 < 150MB、CPU < 2%
- ☐ 用户人工验收

---

## M5 打包分发 ✅

**目标**：能给朋友双击安装使用的 exe 安装包。

### 验收清单
- ☑ `build/pet.spec` 配置 PyInstaller --onefile --windowed
- ☑ PyInstaller 打包成功，得到单个 exe
- ☑ exe 体积 < 80 MB（实测 70 MB）
- ☑ exe 双击可启动，行为与开发期一致
- ☑ `src/core/autostart.py` 实现注册表读写（M3 已就绪，M5 验证：frozen 模式下用 `sys.executable`）
- ☑ 设置面板开机自启开关可写入 `HKCU\...\Run`
- ☑ `build/installer.iss` Inno Setup 中文向导
- ☑ 安装包 < 100 MB（实测 74 MB）
- ☑ 本机干净安装 / 启动 / 卸载流程通跑（受限于单机条件，未在第二台 Windows 上验证）
- ☑ 写 `docs/用户手册.md`
- ☑ 用户人工验收通过

---

## 验收记录

> 每个里程碑通过后，在此追加一条记录。

- **2026-05-23 M1 验收通过**：用户确认透明 / 拖动 / 退出 / 可见 4 项均通过；追加单实例锁与启动器后再次验收通过
- **2026-05-23 M1.5 验收通过**：治理文件齐全，CLAUDE.md / dev-log / docs 全部就位
- **2026-05-23 M2 验收通过**：6 状态切换流畅，巡游 + 拖动 + 边界回弹全部正常
- **2026-05-23 M3 验收通过**：右键菜单 + 抚摸 + 喂食 + 托盘 + 设置面板全部 OK，config.json 持久化生效
- **2026-05-23 M4 验收通过**：AI 流式对话 + 6 厂商预设 + cron 提醒气泡全部跑通
- **2026-05-25 M4.5 验收通过**：用户提交 28 PNG 素材后接入，去白底 + 像素 UI + 像素字体一致风格
- **2026-05-25 M4.6 验收通过**：主动搭话 + 学习陪伴锁定 + 5 分钟警告 + 周期鼓励 + UI 打磨（眨眼/动作频率降低、复选框打钩、tooltip、休息时间 opt-in、设置压气泡）四轮 bugfix 后全部通过
- **2026-05-25 M4.7 验收通过**：keyring 把 API Key 移到 Windows 凭据管理器；旧版 config 自动迁移成功（config.api_key 35→0、keyring 0→35）；设置面板"打开配置文件夹"按钮 + Key 存储位置文案 + 聊天历史保留提示全部生效
- **2026-05-26 M4.8 Phase A 完成**：状态机扩展到 8 状态（加 peek/eat）；feeding 重构为 food_type 分流 + 三段链路（eat 3s → happy 0.8s → idle）；右键"喂食"改子菜单（骨头/狗粮）；`_snap_to_edge` 改切 peek；4 套美术素材 prompts 写入 docs/美术素材清单.md。代码层 6 项已自动验证（AST + 重启 + 内存 ~133MB），UI 层待用户人工验收
- **2026-05-26 M4.9 Phase A 完成**：状态机再扩展到 10 状态（加 hold_bone/fall）；feeding 加 eating_finished Signal + animator 注入，dogfood 位置按朝向、骨头改进入 hold_bone 状态；IdleChatter 加 say_event(category) 通道，喂食结束自动出气泡（after_feed_bone / after_feed_dogfood 各 6 条语料）；pet_window 修 5 bug（contextMenu 漏 cancel / destroyed 时序 / EDGE 露出 54→90 / 拖动后摇 800→350ms / 落地切 fall 状态）；3 套新美术 prompts（peek v2 覆盖 + hold_bone 4 + fall 2 = 9 张）写入 docs。AST 6 文件 + 2 JSON 通过；桌宠重启成功 PID 26428，内存 145MB
- **2026-05-26 M4.8 验收通过**：M4.8 Phase A 与 M4.9 一/二/三/四轮一并验收通过。代码 + 占位 + prompts 全部就绪，UI 体验符合预期（右键子菜单、骨头飞落 + 走过去 + eat、狗粮直接落脚边 + eat、peek 吸边切状态、focus_lock 拦截），待 Phase B 美术替换后自动接入
- **2026-05-26 M4.9 验收通过**：四轮迭代覆盖初版 5 bug + 4 新增（一轮）→ 4 二轮 bug（吸边交互守卫 + 个性化气泡 / peek 镜像反转零改图 / PET_SIZE 160→128 / 喂骨头闪烁修 behavior.pause）→ 2 三轮 bug（dizzy sprite 1.4× 放大 / 狗粮闪烁深挖到 contextMenu aboutToHide resume 时序）→ 2 四轮 bug（dizzy 不裁剪改窗口 dynamic resize 179×179 / peek_wave 18-35s 随机调度 + 招呼气泡 pick_phrase 绕守卫）。最终桌宠 PID 24996，内存 97MB（< 150MB 上限），全部人工验收通过
- **2026-05-26 M5 验收通过**：M5 打包分发全流程通过。Stage 1 建 .gitignore + 更新 requirements.txt + 生成 xiledi.ico；Stage 2 PyInstaller 打 onefile exe（build/dist/XiLeDi.exe 70 MB，< 80 MB 上限），含 keyring.backends.Windows + win32ctypes + openai hiddenimports，excludes 瘦身 13 个未用 PySide6 模块 + tkinter，字体只挂 zh_hans（省 27 MB）；解决 PySide6.QtMultimedia 不能 exclude 的坑（audio.py 顶部 import QSoundEffect 即使 sounds/ 空目录也会触发）；Stage 3 winget 装 Inno Setup 6.7.2 + 手动下载社区版 ChineseSimplified.isl；Stage 4 ISCC 编译 XiLeDi-Setup-1.0.0.exe（74 MB，< 100 MB 上限），中文向导 + 用户级安装（`%LOCALAPPDATA%\Programs\XiLeDi` 免 UAC）+ 桌面/开始菜单快捷方式 + 可选开机自启 + 卸载入口；Stage 5 写 docs/用户手册.md（12 节含安装/配置 AI/学习陪伴/提醒/卸载/FAQ）。本机干净安装 → 启动 → 卸载全流程通跑，用户人工验收通过
